# ⚡ БЫСТРАЯ СПРАВКА

## 🚀 ЗАПУСК (выбери одно)

### Windows

```cmd
run.bat
```

### macOS/Linux

```bash
python3 run.py
```

### VS Code (F5)

Нажми F5 и выбери конфигурацию

---

## 🎮 ДОСТУП

```
http://localhost:5000/crash
```

---

## 🔧 ЕСЛИ НЕ РАБОТАЕТ

1. **Установи зависимости**

   ```
   pip install flask flask-cors
   ```

2. **Проверь Python**

   ```
   python --version
   ```

   (Должно быть 3.8+)

3. **Перезагрузи сервер**

   ```
   Ctrl+C (остановить)
   Запустить снова
   ```

4. **Очисти кэш браузера**
   ```
   Ctrl+Shift+Delete
   ```

---

## 📝 ФАЙЛЫ

- `app.py` - главное приложение ✅ исправлено
- `templates/crash.html` - crash режим ✅ исправлено
- `requirements.txt` - зависимости
- `run.py` / `start.py` - скрипты запуска
- `.vscode/` - конфиг VS Code

---

## 💡 ПОЛЕЗНЫЕ КОМАНДЫ

Открыть логи Flask:

```
python -m flask --app app run -v
```

Переустановить зависимости:

```
pip install --upgrade -r requirements.txt
```

Удалить БД и начать заново:

```
del data\raswet_gifts.db
```

---

## 🎉 ГОТОВО!

Просто запусти скрипт и наслаждайся игрой! 🚀
