# ⚡ ЗАГРУЗКА В GITHUB ЗА 2 МИНУТЫ

## 🎯 САМЫЙ БЫСТРЫЙ ПУТЬ

### Способ 1: Автоматический скрипт (РЕКОМЕНДУЕТСЯ)

#### Windows CMD:

```batch
upload_to_github.bat
```

#### Windows PowerShell:

```powershell
.\upload_to_github.ps1
```

Скрипт автоматически:

- ✅ Инициализирует Git
- ✅ Добавляет все файлы
- ✅ Создает первый коммит

---

### Способ 2: Вручную (3 команды)

```bash
# 1. Инициализируем
git init
git config user.name "Твое имя"
git config user.email "твой@email.com"

# 2. Добавляем файлы
git add .
git commit -m "Initial commit: RasswetGifts"

# 3. Подключаем к GitHub (замени ТВО_НИК!)
git remote add origin https://github.com/ТВО_НИК/raswet-gifts.git
git branch -M main
git push -u origin main
```

---

## 📋 ПОШАГОВО

### Шаг 1: GitHub репозиторий (1 минута)

1. Переходишь: https://github.com/new
2. Заполняешь:
   - Repository name: **raswet-gifts**
   - Visibility: **Public**
3. Нажимаешь **Create repository**
4. ⚠️ НЕ инициализируй с README!

### Шаг 2: Загружаешь код (1 минута)

Копируешь 3 команды из GitHub:

```
git remote add origin https://github.com/ТВО_НИК/raswet-gifts.git
git branch -M main
git push -u origin main
```

Вставляешь в терминал и пушишь Enter.

### Шаг 3: Готово! ✅

- Твой код на GitHub
- Все файлы загружены
- Можешь подключить к Railway

---

## 🔐 Аутентификация GitHub

Когда Git попросит пароль:

**Вариант 1: Personal Access Token (РЕКОМЕНДУЕТСЯ)**

1. Переходишь: https://github.com/settings/tokens
2. Нажимаешь "Generate new token"
3. Выбираешь "Classic"
4. Ставишь галочку на `repo`
5. Копируешь токен
6. Вставляешь как пароль в Git

**Вариант 2: SSH ключ** (продвинутый вариант)

```bash
ssh-keygen -t ed25519 -C "твой@email.com"
```

Затем добавляешь публичный ключ в GitHub Settings.

---

## ✅ ПРОВЕРКА

После успешного пуша:

1. Открываешь: https://github.com/ТВО_НИК/raswet-gifts
2. Видишь все папки и файлы
3. Видишь сообщение коммита

Если видишь все это - значит всё правильно! 🎉

---

## 🚨 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

### Ошибка: "fatal: not a git repository"

```bash
git init
```

### Ошибка: "fatal: remote origin already exists"

```bash
git remote remove origin
git remote add origin https://github.com/ТВО_НИК/raswet-gifts.git
```

### Ошибка: "Permission denied"

- Используй Personal Access Token (не пароль!)
- Или создай SSH ключ

### Ошибка: ".env файл случайно загружен"

```bash
git rm --cached .env
git commit -m "Remove .env file"
git push
```

---

## 📊 ЧТО БУДЕТ ЗАГРУЖЕНО

✅ Загружается:

- `app.py`, `bot.py` - основной код
- `templates/` - HTML шаблоны
- `static/` - статические файлы
- `data/cases.json` и другие конфиги
- `requirements.txt` - зависимости
- `Procfile`, `Dockerfile`, `railway.json` - конфиги для Railway
- Вся документация

❌ НЕ загружается:

- `.env` - конфиденциальные переменные
- `data/raswet_gifts.db` - база данных
- `__pycache__/`, `*.pyc` - кэш Python
- `venv/` - виртуальное окружение

(Всё это в `.gitignore`)

---

## 🎉 ДАЛЬШЕ

Когда код на GitHub:

1. Переходишь на Railway
2. Создаешь новый проект
3. Подключаешь этот GitHub репозиторий
4. Railway автоматически начнет деплой

**Вот и всё!** 🚀

---

**Статус:** ⚡ БЫСТРАЯ ЗАГРУЗКА ГОТОВА
