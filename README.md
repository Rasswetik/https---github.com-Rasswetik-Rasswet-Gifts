# 🎮 RasswetGifts - Crash Game Mode

Прекрасная игра для открытия подарков через Telegram Mini App с режимом **CRASH** - динамичной игре со взлетающей ракетой!

## ✨ Возможности

- 🚀 **CRASH режим** - тратьте звезды на ставки и выигрывайте подарки
- 🎁 **Система кейсов** - открывайте кейсы и получайте награды
- ⭐ **Система уровней** - прогрессируйте и получайте награды
- 💰 **Реферальная система** - приглашайте друзей и получайте бонусы
- 🎯 **Улучшение персонажа** - развивайте своего персонажа

## 🚀 Быстрый старт

### Требования

- Python 3.8+
- pip

### Установка и запуск

1. **Откройте терминал в папке проекта**

   ```bash
   cd "c:\Users\Игорь\Desktop\RasswetGifts"
   ```

2. **Запустите приложение**

   ```bash
   python run.py
   ```

   Это автоматически:
   - ✅ Установит все зависимости
   - ✅ Инициализирует базу данных
   - ✅ Запустит Flask сервер

3. **Откройте браузер**
   ```
   http://localhost:5000
   ```

## 📁 Структура проекта

```
RasswetGifts/
├── app.py                 # Основное Flask приложение
├── bot.py                 # Telegram bot интеграция
├── run.py                 # Скрипт запуска
├── requirements.txt       # Python зависимости
│
├── templates/             # HTML шаблоны
│   ├── crash.html        # ✨ CRASH режим (ИСПРАВЛЕНО)
│   ├── index.html        # Главная страница
│   ├── lobby.html        # Лобби
│   └── ...
│
├── static/               # Статические файлы
│   ├── gifs/            # GIF анимации
│   │   ├── gifts/       # GIF подарков
│   │   └── crash.gif    # GIF ракеты
│   └── uploads/         # Загруженные файлы
│
└── data/                # Данные приложения
    ├── cases.json       # Конфигурация кейсов
    ├── crash_status.json # Статус crash игр
    └── raswet_gifts.db  # SQLite база данных
```

## 🛠️ Исправления в crash.html

Были исправлены следующие ошибки:

1. ✅ **Undefined переменная `amountInput`** → Теперь использует `document.getElementById("amount")`
2. ✅ **Неправильный endpoint** → `/api/telegram/user` → `/api/crash/status`
3. ✅ **Обработка ошибок** → Добавлены try-catch блоки
4. ✅ **Валидация данных** → Проверка корректности суммы ставки
5. ✅ **Улучшена визуализация** → Более красивые сообщения об успехе

## 🎮 Как работает CRASH режим

### Основные правила:

1. 🎯 **Сделайте ставку** звездами (10, 50, 100, 500 или любое число)
2. 🚀 **Смотрите как ракета летит** - множитель растет
3. 💰 **Нажмите ЗАБРАТЬ** перед краком, чтобы получить выигрыш
4. 💥 **Если пропустили** - ракета упадет и вы потеряете ставку

### API Endpoints для Crash:

```
GET  /api/crash/status       - Получить статус текущей игры
POST /api/crash/bet          - Сделать ставку
POST /api/crash/cashout      - Забрать выигрыш
```

## ⚙️ Конфигурация

### Переменные окружения (опционально)

Создайте файл `.env`:

```
FLASK_ENV=development
FLASK_DEBUG=True
PORT=5000
```

### Основные переменные в app.py:

```python
BASE_PATH = os.path.dirname(os.path.abspath(__file__))  # Автоматически определяется
ADMIN_ID = 5257227756                                   # ID администратора
TELEGRAM_BOT_TOKEN = '...'                             # Токен Telegram бота
```

## 🐛 Отладка

Если приложение не запускается:

1. **Проверьте Python**

   ```bash
   python --version
   ```

   Должна быть версия 3.8 или выше.

2. **Проверьте зависимости**

   ```bash
   pip install -r requirements.txt
   ```

3. **Проверьте порт**

   ```bash
   netstat -ano | findstr :5000
   ```

   Если порт занят, измените в `app.py`:

   ```python
   app.run(host='127.0.0.1', port=5001, debug=True)
   ```

4. **Посмотрите логи**
   Все ошибки выводятся в консоль при запуске с `debug=True`

## 📊 База данных

Приложение автоматически создает SQLite БД с таблицами:

- `users` - данные пользователей
- `inventory` - инвентарь пользователей
- `crash_games` - игры crash режима
- `crash_bets` - ставки в crash
- `crash_history` - история crash игр
- И другие...

## 🎨 UI/UX

CRASH режим имеет:

- 📱 Адаптивный дизайн для мобильных устройств
- ✨ Красивые анимации
- 🎭 Темная тема
- ⚡ Быстрая загрузка

## 🤝 Контрибьютинг

Если вы нашли ошибку или хотите добавить функционал:

1. Создайте issue
2. Создайте fork
3. Отправьте pull request

## 📝 Лицензия

Все права на этот проект принадлежат создателю.

---

**Приятной игры! 🎉**
