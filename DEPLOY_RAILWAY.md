# 🚀 Деплой на Railway через GitHub

Полная инструкция для развертывания приложения RasswetGifts на Railway.

## 📋 Требования

- GitHub аккаунт (бесплатно на github.com)
- Railway аккаунт (бесплатно на railway.app)
- Git установлен на компьютере

---

## 🎯 Пошаговая инструкция

### Шаг 1: Подготовка к GitHub

#### 1.1 Инициализируем Git репозиторий

```bash
cd "c:\Users\Игорь\Desktop\RasswetGifts"
git init
git config user.name "Твое имя"
git config user.email "твой.email@example.com"
```

#### 1.2 Создаем .gitignore файл (уже есть, проверь)

Должны быть исключены:

- `__pycache__/`
- `*.pyc`
- `.env` (конфиденциальные переменные!)
- `data/raswet_gifts.db`
- `venv/`

#### 1.3 Добавляем файлы в Git

```bash
git add .
git commit -m "Initial commit: RasswetGifts Crash Game"
```

### Шаг 2: Создание репозитория на GitHub

#### 2.1 Переходим на GitHub

Откройте: https://github.com/new

#### 2.2 Заполняем форму

```
Repository name: raswet-gifts
Description: Crash game Telegram web app with Flask
Public или Private: Выбери Public
```

#### 2.3 Создаем репозиторий (не инициализируй с README)

#### 2.4 Добавляем удаленный репозиторий

```bash
git remote add origin https://github.com/ТВО_НИК/raswet-gifts.git
git branch -M main
git push -u origin main
```

> Замени `ТВО_НИК` на твой GitHub никнейм

### Шаг 3: Настройка переменных окружения

#### 3.1 Копируем .env.example в .env

```bash
copy .env.example .env
```

#### 3.2 Редактируем .env

Замени в файле `.env`:

```env
FLASK_ENV=production
FLASK_DEBUG=False
HOST=0.0.0.0
PORT=8000
TELEGRAM_BOT_TOKEN=8224991617:AAF2F7ub0XF9N6wsWyn3PmhdZnYt62KmpRE
ADMIN_ID=5257227756
SECRET_KEY=your-super-secret-key-generate-random
WEBSITE_URL=https://raswet-gifts.up.railway.app
```

> **ВАЖНО**: Никогда не коммитьте `.env` файл! Он в `.gitignore`.

#### 3.3 Генерируем SECRET_KEY

```python
python -c "import secrets; print(secrets.token_hex(32))"
```

Копируешь результат в `SECRET_KEY` в `.env`.

### Шаг 4: Деплой на Railway

#### 4.1 Переходим на Railway

Откройте: https://railway.app

#### 4.2 Логинимся через GitHub

- Нажимаем "Login with GitHub"
- Даем разрешения

#### 4.3 Создаем новый проект

- Нажимаем "+ New Project"
- Выбираем "Deploy from GitHub repo"

#### 4.4 Подключаем репозиторий

- Выбираем свой репозиторий `raswet-gifts`
- Railway автоматически обнаружит `Procfile` и `Dockerfile`

#### 4.5 Добавляем переменные окружения в Railway

В панели Railway:

1. Переходим на вкладку "Variables"
2. Добавляем переменные из файла `.env`:

```
FLASK_ENV=production
FLASK_DEBUG=False
HOST=0.0.0.0
PORT=8000
TELEGRAM_BOT_TOKEN=8224991617:...
ADMIN_ID=5257227756
SECRET_KEY=<результат из шага 4.3>
WEBSITE_URL=https://<твой-домен>.up.railway.app
```

#### 4.6 Деплой

Railway автоматически начнет деплой при добавлении переменных.

Следи за логами в консоли Railway:

- 🔨 "Building..."
- ✅ "Deployment successful"
- 🚀 Сайт доступен!

---

## 🔗 Получение адреса сайта

После успешного деплоя:

1. Переходишь на вкладку "Deployments"
2. Там будет ссылка вида: `https://raswet-gifts-xxx.up.railway.app`
3. Это твой рабочий сайт! 🎉

---

## 📱 Использование с Telegram

1. Замени `https://rasswetik52.pythonanywhere.com` на `https://raswet-gifts-xxx.up.railway.app` везде в коде бота
2. Переразверни приложение (еще один push на GitHub)
3. Telegram веб-приложение будет использовать новый URL

---

## 🔄 Как обновлять сайт

Каждый раз когда ты делаешь изменения:

```bash
git add .
git commit -m "Описание изменений"
git push origin main
```

Railway автоматически:

1. 🔔 Заметит новый push
2. 🔨 Запустит новый деплой
3. ✅ Обновит твой сайт

---

## 🐛 Если что-то не работает

### Ошибка: "Build failed"

Посмотри логи в Railway:

1. Перейди на "Deployments"
2. Нажми на неудачный деплой
3. Скопируй ошибку

Частые проблемы:

- Не установлены зависимости → Проверь `requirements.txt`
- Синтаксическая ошибка в Python → Чекну свой код
- Неправильные переменные окружения → Проверь в Railway

### Ошибка: "Application crashed"

1. Посмотри логи в "Logs" вкладке Railway
2. Обычно ошибка в коде Python
3. Исправь локально и запуши снова

### Сайт работает, но Telegram вебприложение не загружается

1. Убедись что в Railway выставлена переменная `WEBSITE_URL` с правильным адресом
2. Проверь что Telegram бот имеет правильный webhook URL
3. Перезагрузи telegram веб-приложение (Ctrl+R)

---

## 💾 Резервная копия данных

Railway хранит данные в `/app/data/raswet_gifts.db`, но при переразвертывании они удаляются!

Для сохранения данных:

1. **Вариант 1: PostgreSQL на Railway**
   - Создай PostgreSQL базу через Railway
   - Подключи к приложению
   - Данные сохранятся

2. **Вариант 2: Скачивать резервные копии**

   ```bash
   # Скачать БД с сервера через SSH
   # (требует платный тариф Railway)
   ```

3. **Вариант 3: GitHub LFS** (для маленьких БД)
   - Коммитьте БД в git
   - GitHub будет ее хранить

Рекомендую вариант 1 (PostgreSQL).

---

## 🔐 Безопасность

### Перед публикацией:

- ✅ Никогда не коммитьте `.env`
- ✅ Никогда не делитесь `SECRET_KEY`
- ✅ Никогда не публикуйте токены в коде
- ✅ Используйте переменные окружения
- ✅ Установите правильные CORS headers

### Рекомендации:

```python
# Уже исправлено в app.py!
TELEGRAM_BOT_TOKEN = os.getenv('TELEGRAM_BOT_TOKEN')
SECRET_KEY = os.getenv('SECRET_KEY')
```

---

## 📊 Мониторинг

В панели Railway видишь:

- 📈 CPU/Memory usage
- 📊 Количество запросов
- 💾 Размер базы данных
- 🔥 Статус приложения

---

## 💰 Стоимость

**Railway бесплатно предоставляет:**

- 🎁 $5 в месяц бесплатных кредитов
- 🎁 Этого хватит на Flask приложение
- 🎁 Неограниченное количество развертываний

Если потребуется больше ресурсов:

- $5/месяц = базовый тариф
- $10/месяц = расширенный
- И выше...

---

## 🎉 Готово!

Теперь у тебя есть:

- ✅ GitHub репозиторий с кодом
- ✅ Автоматический деплой на Railway
- ✅ Рабочий сайт на интернете
- ✅ Автоматические обновления при каждом push

Твой сайт доступен 24/7! 🚀

---

## 📞 Полезные ссылки

- [Railway Documentation](https://docs.railway.app/)
- [GitHub Help](https://docs.github.com/)
- [Flask Deployment](https://flask.palletsprojects.com/deployment/)
- [Gunicorn Documentation](https://gunicorn.org/)

---

**Статус:** ✅ Готово к деплою!
