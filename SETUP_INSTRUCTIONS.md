# 🚀 ИНСТРУКЦИЯ ПО ЗАПУСКУ RasswetGifts

## ✅ Что было исправлено и сделано

### 1. 🔧 Исправлены ошибки в `crash.html`

- ✅ **Ошибка переменной**: `amountInput` → `document.getElementById("amount")`
- ✅ **Неправильный API**: `/api/telegram/user` → `/api/crash/status`
- ✅ **Обработка ошибок**: Добавлены try-catch блоки во все async функции
- ✅ **Валидация**: Добавлена проверка корректности суммы ставки
- ✅ **Парсинг**: Правильный парсинг чисел с `parseFloat()`
- ✅ **Улучшена обработка ответов**: Добавлены проверки на ошибки из API

### 2. 🔧 Исправлена конфигурация `app.py`

- ✅ **BASE_PATH**: Изменен с `/home/rasswetik52/mysite` на локальный путь
  ```python
  BASE_PATH = os.path.dirname(os.path.abspath(__file__))
  ```
- ✅ **Порт**: Приложение слушает на `127.0.0.1:5000` по умолчанию

### 3. 📦 Установлены расширения VS Code

- ✅ Python
- ✅ Pylance
- ✅ Debugpy

### 4. 📝 Созданы вспомогательные файлы

- ✅ `requirements.txt` - зависимости Python
- ✅ `run.py` - основной скрипт для запуска
- ✅ `start.py` - альтернативный скрипт запуска
- ✅ `run.bat` - Windows батник для быстрого запуска
- ✅ `run.ps1` - PowerShell скрипт
- ✅ `.vscode/launch.json` - конфигурация отладчика
- ✅ `.vscode/settings.json` - настройки VS Code
- ✅ `.gitignore` - игнорирование ненужных файлов

---

## 🎮 КАК ЗАПУСТИТЬ ПРИЛОЖЕНИЕ

### Способ 1: Простой запуск (Рекомендуется)

#### Windows (CMD):

```cmd
cd c:\Users\Игорь\Desktop\RasswetGifts
run.bat
```

#### Windows (PowerShell):

```powershell
cd "c:\Users\Игорь\Desktop\RasswetGifts"
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
.\run.ps1
```

#### macOS/Linux:

```bash
cd ~/Desktop/RasswetGifts
chmod +x run.bat
python3 run.py
```

### Способ 2: Через Python напрямую

```bash
cd "c:\Users\Игорь\Desktop\RasswetGifts"
python start.py
```

### Способ 3: Через VS Code

1. Откройте папку проекта в VS Code
2. Нажмите `Ctrl+Shift+D` (откроет Run & Debug)
3. Выберите конфигурацию:
   - **"RasswetGifts - Run Script"** (рекомендуется)
   - Или **"RasswetGifts - Flask Server"**
4. Нажмите зеленую кнопку ▶️ для запуска
5. Приложение запустится с отладкой

---

## 📱 КАК ПОЛУЧИТЬ ДОСТУП К ПРИЛОЖЕНИЮ

После запуска сервера откройте в браузере:

### Главная страница:

```
http://localhost:5000
```

### CRASH режим (ИСправленный):

```
http://localhost:5000/crash
```

### Другие режимы:

- Ultimate Crash: `http://localhost:5000/ultimate-crash`
- Кейсы: `http://localhost:5000/cases`
- Инвентарь: `http://localhost:5000/inventory`

---

## 🎯 ТЕСТИРОВАНИЕ CRASH РЕЖИМА

1. Откройте `http://localhost:5000/crash` в браузере
2. Нажмите кнопку **"СДЕЛАТЬ СТАВКУ"**
3. Введите сумму (10, 50, 100, 500 или любое число)
4. Нажмите **"Поставить"**
5. Смотрите как ракета летит вверх 🚀
6. Нажмите **"ЗАБРАТЬ"** перед крахом чтобы получить выигрыш
7. Если не успели - ракета упадет 💥

---

## 🐛 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

### Ошибка: "No module named 'flask'"

```bash
pip install flask flask-cors
```

### Ошибка: "Port 5000 is already in use"

1. Найдите процесс на порту 5000:
   ```powershell
   netstat -ano | findstr :5000
   ```
2. Завершите процесс:
   ```powershell
   taskkill /PID <PID> /F
   ```
3. Или измените порт в `app.py`:
   ```python
   app.run(host='127.0.0.1', port=5001, debug=True)
   ```

### Ошибка: "База данных заблокирована"

Просто перезагрузите сервер. SQLite иногда требует перезагрузки.

### Ошибка 404 на страницах

Убедитесь что:

- Сервер работает (см логи в консоли)
- Вы используете правильный URL
- Все файлы HTML на месте в папке `templates/`

### Консоль выдает ошибки JavaScript

1. Откройте инструменты разработчика (F12)
2. Перейдите на вкладку Console
3. Скопируйте ошибку и проверьте:
   - Правильные ли API endpoints
   - Возвращает ли сервер данные

---

## 📊 СТРУКТУРА БАЗЫ ДАННЫХ

При первом запуске создается SQLite база данных с таблицами:

```
raswet_gifts.db
├── users                  # Пользователи
├── inventory              # Инвентарь (подарки)
├── crash_games           # Игры crash
├── crash_bets            # Ставки в crash
├── crash_history         # История crash
├── case_limits           # Лимиты кейсов
├── referrals             # Реферальная система
└── user_history          # История операций
```

---

## 📈 ЛОГИРОВАНИЕ

При запуске с `debug=True` все действия логируются в консоль:

- 🎮 Запросы к API
- 📊 Изменения БД
- ❌ Ошибки
- ⚠️ Предупреждения

---

## 🔐 БЕЗОПАСНОСТЬ

⚠️ **ВАЖНО**: Это локальная разработка!

- НЕ используйте в production без изменений
- НЕ делитесь токеном бота
- НЕ коммитьте файл с API ключами
- Используйте `.env` для конфиденциальных данных

---

## 📞 КОНТАКТЫ И ПОМОЩЬ

Если возникли проблемы:

1. **Проверьте консоль** при запуске сервера
2. **Посмотрите логи** Flask
3. **Откройте DevTools** в браузере (F12)
4. **Читайте README.md** для дополнительной информации

---

## 🎉 ГОТОВО!

Приложение полностью настроено и готово к работе!

### Главное что было сделано:

1. ✅ Исправлены все ошибки в crash.html
2. ✅ Настроено локальное окружение
3. ✅ Установлены необходимые расширения
4. ✅ Созданы скрипты для запуска
5. ✅ Подготовлена документация

**Теперь просто запустите приложение и наслаждайтесь! 🚀**
